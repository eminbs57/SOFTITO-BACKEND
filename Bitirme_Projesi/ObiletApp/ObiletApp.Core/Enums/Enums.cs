namespace ObiletApp.Core.Enums
{
    public enum VehicleType { Bus = 1, Airplane = 2 }
    public enum TicketStatus { Active = 1, Cancelled = 2, Used = 3 }
    public enum Gender { Male = 1, Female = 2, Other = 3 }
    public enum PaymentMethod { CreditCard = 1, BankTransfer = 2 }
}