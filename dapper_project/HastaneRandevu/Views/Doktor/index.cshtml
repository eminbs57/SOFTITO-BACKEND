@model IEnumerable<HastaneRandevu.Models.DoktorModel>
@{
    ViewData["Title"] = "Doktorlar";
    var poliklinikler = ViewBag.Poliklinikler as IEnumerable<HastaneRandevu.Models.PoliklinikModel> ?? new List<HastaneRandevu.Models.PoliklinikModel>();
}

<div class="container-fluid py-3">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3 class="fw-bold text-dark mb-0">Doktor Listesi</h3>
        <a href="/Doktor/EY" class="btn btn-primary shadow-sm"><i class="bi bi-person-plus-fill me-1"></i> Yeni Doktor Ekle</a>
    </div>

    <div class="card card-modern">
        <div class="card-header bg-white border-bottom py-3 d-flex justify-content-end align-items-center">
            <form method="get" action="/Doktor/Index" class="d-flex w-100" style="max-width: 400px;">
                <div class="input-group">
                    <span class="input-group-text bg-white text-muted border-end-0"><i class="bi bi-search"></i></span>
                    <input type="text" name="searchTerm" class="form-control border-start-0 ps-0" placeholder="Ad Soyad ile ara..." value="@ViewBag.SearchTerm" />
                    <button type="submit" class="btn btn-outline-primary">Ara</button>
                    @if(!string.IsNullOrEmpty((string)ViewBag.SearchTerm)) {
                        <a href="/Doktor/Index" class="btn btn-outline-secondary"><i class="bi bi-x-lg"></i></a>
                    }
                </div>
            </form>
        </div>
        
        <div class="table-responsive">
            <table class="table table-modern table-hover align-middle">
                <thead>
                    <tr>
                        <th>Ad Soyad</th>
                        <th>Unvan</th>
                        <th>Poliklinik Adı</th>
                        <th class="text-end">İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="fw-semibold">@item.AdSoyad</td>
                            <td><span class="badge bg-light text-dark border">@item.Unvan</span></td>
                            <td>@(poliklinikler.FirstOrDefault(p => p.PoliklinikNo == item.PoliklinikNo)?.PoliklinikAdi ?? item.PoliklinikNo.ToString())</td>
                            <td class="text-end">
                                <a href="/Doktor/EY/@item.DoktorNo" class="btn btn-outline-secondary icon-btn shadow-sm" title="Düzenle">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <a href="/Doktor/Delete/@item.DoktorNo" class="btn btn-outline-danger icon-btn shadow-sm ms-1" onclick="return confirm('Bu doktoru silmek istediğinize emin misiniz?');" title="Sil">
                                    <i class="bi bi-trash"></i>
                                </a>
                            </td>
                        </tr>
                    }
                    @if(!Model.Any()) {
                        <tr>
                            <td colspan="4" class="text-center py-4 text-muted">Kayıt bulunamadı.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>